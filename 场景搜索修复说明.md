# 场景搜索修复说明

## 问题描述

用户反馈:"场景搜索有问题, 办公显示器出来的是得力的笔, 游戏显示器出来的并不是144hz的"

## 问题原因

原来的同义词扩展逻辑存在以下问题:

1. **过度泛化的同义词匹配**
   - `synonymDict` 中包含了 `'办公': ['商用', '企业']` 这样的泛词
   - 搜索"办公显示器"时,"办公"会被扩展到"商用"、"企业"
   - 导致"办公用品"(得力文具)也被匹配上

2. **缺少场景专属评分**
   - 搜索"游戏显示器"时,虽然匹配到了显示器
   - 但没有针对游戏场景(144Hz刷新率)的额外评分
   - 导致普通显示器和游戏显示器分数相近,排序不准确

## 解决方案

### 1. 引入组合关键词优先匹配

在 `smartSearch.js` 中新增 `combinedKeywords` 对象:

```javascript
const combinedKeywords = {
  '办公显示器': ['商用显示器', '企业显示器', 'office monitor', '办公屏'],
  '游戏显示器': ['电竞显示器', 'gaming monitor', '游戏屏', '电竞屏'],
  '设计显示器': ['专业设计', '设计屏', 'design monitor']
};
```

### 2. 重构 `expandSynonyms` 函数

**修改前逻辑:**
```javascript
function expandSynonyms(keyword) {
  // 直接分词,然后查找所有同义词
  // 导致"办公"匹配过于宽泛
}
```

**修改后逻辑:**
```javascript
function expandSynonyms(keyword) {
  // 1. 优先检查组合关键词
  if (keyword包含"办公显示器") {
    return ['办公显示器', '商用显示器', '企业显示器', 'office monitor', '办公屏']
    // 不再继续泛化扩展
  }

  // 2. 如果匹配到组合关键词,仍然对"显示器"、"2K"等专有名词扩展同义词

  // 3. 如果没有匹配组合关键词,才进行常规同义词扩展
}
```

**关键改进点:**
- 搜索"办公显示器"时,只扩展到显示器相关的办公类型
- 搜索"游戏显示器"时,只扩展到显示器相关的游戏/电竞类型
- **避免"办公"单独扩展到"商用"、"企业",匹配到文具**

### 3. 增加场景匹配评分

在 `calculateMatchScore` 函数中新增场景评分逻辑:

```javascript
// 场景匹配(显示器) - 根据场景关键词匹配specs.scene
if (productSpecs.scene) {
  // 游戏/电竞显示器匹配
  if (keyword包含'游戏/电竞/gaming' && scene === '游戏') {
    score += 20; // 场景匹配
    if (refreshRate >= 144) {
      score += 15; // 高刷新率额外加分
    }
  }

  // 办公显示器匹配
  if (keyword包含'办公/商用/企业' && scene === '办公') {
    score += 20;
  }

  // 设计显示器匹配
  if (keyword包含'设计/专业' && scene === '设计') {
    score += 20;
    if (resolution包含'2K/4K') {
      score += 10; // 高分辨率额外加分
    }
  }
}
```

**评分权重:**
- 场景匹配: +20分
- 游戏场景 + 144Hz+: 额外+15分 (共35分)
- 设计场景 + 2K/4K: 额外+10分 (共30分)

## 测试验证

### 测试1: 办公显示器搜索

```
搜索: "办公显示器"

预期结果:
✅ 只匹配显示器,不匹配文具
✅ 返回23.8"-27"办公型号
✅ 刷新率60-75Hz
✅ 价格700-1500元

验证方法:
1. 打开 http://localhost:8080/
2. 搜索"办公显示器"
3. 查看结果是否全部为显示器
4. 检查控制台日志中的匹配关键词
```

### 测试2: 游戏显示器搜索

```
搜索: "游戏显示器"

预期结果:
✅ 返回144Hz+的高刷新率显示器
✅ 主要是27寸 2K规格
✅ 匹配分数高(场景20分+高刷15分+关键词15分=50+)
✅ 排序优先显示144Hz/165Hz型号

验证方法:
1. 搜索"游戏显示器"
2. 查看结果的refreshRate字段
3. 应全部≥144Hz
4. 查看匹配分数(matchScore)应较高
```

### 测试3: 设计显示器搜索

```
搜索: "设计显示器"

预期结果:
✅ 返回27"-31.5"大尺寸
✅ 分辨率2K或4K
✅ 高分辨率型号排名靠前

验证方法:
1. 搜索"设计显示器"
2. 查看分辨率字段
3. 应包含2560×1440或3840×2160
```

## 修改的文件

- `/home/intel41/mall-app-web/mall-pc/src/utils/smartSearch.js`
  - 新增 `combinedKeywords` 对象
  - 重构 `expandSynonyms` 函数(优先匹配组合关键词)
  - 增强 `calculateMatchScore` 函数(场景匹配评分)

## 技术细节

### 同义词扩展优先级

```
1. 组合关键词(最高优先级)
   "办公显示器" → ['办公显示器', '商用显示器', '企业显示器', ...]

2. 专有名词同义词(中优先级)
   "显示器" → ['屏幕', 'monitor', 'display']
   "2K" → ['2560×1440', 'QHD']

3. 常规分词同义词(低优先级)
   仅在未匹配组合关键词时执行
```

### 匹配分数构成

```
总分 = 关键词匹配(0-50分)
     + 规格匹配(0-30分)
     + 场景匹配(0-35分)
     + 分类匹配(0-10分)
     + 热度加权(0-10分)

游戏显示器最高分:
- 关键词"游戏显示器"匹配: 15分
- 尺寸27寸匹配: 30分
- 刷新率144Hz匹配: 25分
- 场景"游戏"+高刷: 35分
- 分类"显示器": 10分
= 共115分
```

## 预期效果

| 搜索词 | 修复前 | 修复后 |
|--------|-------|--------|
| 办公显示器 | ❌ 返回文具+显示器混合 | ✅ 只返回办公型显示器 |
| 游戏显示器 | ❌ 返回所有显示器 | ✅ 优先返回144Hz+游戏显示器 |
| 设计显示器 | ❌ 返回所有显示器 | ✅ 优先返回2K/4K高分辨率 |

## 后续优化建议

1. **增加更多场景关键词**
   - 例如: "带鱼屏"、"曲面屏"、"便携显示器"

2. **学习用户搜索行为**
   - 记录用户点击的商品
   - 根据点击率优化同义词词典

3. **A/B测试评分权重**
   - 测试不同的场景匹配分数
   - 优化用户体验

---

**修复完成时间**: 2025-12-04
**开发服务器**: http://localhost:8080/
**测试指南**: 参见 `显示器AI搜索测试指南.md`
